# Epic E1 Test Report — 2026-06-02

## Summary

| Metric | Value |
|---|---|
| Sprint | S1.1 + S1.2 + S1.3 (Epic E1 complete) |
| Date | 2026-06-02 |
| LIVE checks | 14 |
| STATIC checks | 1 |
| BLOCKED | 0 |
| Overall verdict | **PASS** |

## Environment

| Item | Value |
|---|---|
| Server | FastAPI + uvicorn, port 8000 |
| Python | 3.12.11 |
| Start command | `.venv/bin/python3.12 -m uvicorn src.main:app --port 8000` |
| Server status | Started cleanly — "Application startup complete" |
| Network | Hedera testnet (testnet.mirrornode.hedera.com) |

---

## Sprint Items Verified

### S1.1 — Runnable FastAPI + MCP Server

```
[PASS]  AC1: GET /health returns {"status":"ok"}                      LIVE
  Given: Server running on port 8000
  When:  curl -s http://localhost:8000/health
  Then:  HTTP 200, body {"status":"ok"}
  Got:   200 {"status":"ok"}

[PASS]  AC2: POST /mcp/tools returns tools list                       LIVE
  Given: Server running
  When:  curl -s -X POST http://localhost:8000/mcp/tools
  Then:  HTTP 200, body contains "tools" array
  Got:   200 {"tools":[{"name":"execute-static-analysis",...}]}

[PASS]  AC3: Server starts cleanly                                    LIVE
  Given: .env fully populated
  When:  .venv/bin/python3.12 -m uvicorn src.main:app --port 8000
  Then:  "Application startup complete" with no errors
  Got:   Confirmed — no errors in startup output

[PASS]  AC4: Concurrent requests — no blocking                        LIVE
  Given: Server running
  When:  Two parallel curl calls to /health
  Then:  Both return 200
  Got:   200 {"status":"ok"} × 2
```

### S1.2 — Credential Vault

```
[PASS]  AC1: .env loaded at startup via python-dotenv                 LIVE
  Given: .env present in mcp-server/
  When:  python -c "from src.config import settings; print(list(settings.keys()))"
  Then:  All 6 required keys present in settings dict
  Got:   ['HEDERA_NETWORK', 'HEDERA_SERVER_ACCOUNT_ID', 'HEDERA_SERVER_PRIVATE_KEY',
          'HCS_AUDIT_TOPIC_ID', 'MIRROR_NODE_URL', 'TOOL_PRICE_HBAR']

[PASS]  AC2: Keys accessible to internal modules                      STATIC
  Given: config.py exposes settings dict
  When:  Code review — settings imported in main.py
  Then:  Keys available to any module that imports settings
  Notes: Confirmed by pattern — validated by AC1 (keys present in settings dict)

[PASS]  AC3: No key in any HTTP response body                         LIVE
  Given: Server running with real private key in .env
  When:  Captured body of /health, POST /mcp/tools, POST /mcp/tools/execute-static-analysis
  Then:  None of the three responses contain the private key string
  Got:   grep for key prefix (302e020100...) — not found in any response

[PASS]  AC4: Fail-fast on missing required key                        LIVE
  Given: HEDERA_SERVER_PRIVATE_KEY overridden to empty string
  When:  python -c "from src.config import settings"
  Then:  RuntimeError naming the missing key
  Got:   RuntimeError: Required environment variable not set: HEDERA_SERVER_PRIVATE_KEY

[PASS]  AC5: .env is in .gitignore                                    STATIC
  Given: mcp-server/.gitignore
  When:  cat .gitignore | grep .env
  Then:  .env listed
  Got:   Confirmed present in prior session
```

### S1.3 — First Premium Tool: Static Code Analysis

```
[PASS]  AC1: Tool registered — name + description in tools list       LIVE
  Given: Server running
  When:  curl -s -X POST http://localhost:8000/mcp/tools
  Then:  tools array contains {name: "execute-static-analysis", description: "..."}
  Got:   Confirmed — name and description present

[PASS]  AC2: Happy path — valid Python returns 200 with issues        LIVE
  Given: code="import os\nx = 1\n", language="python"
  When:  POST /mcp/tools/execute-static-analysis
  Then:  HTTP 200, issues array, summary with totals
  Got:   200 — pyflakes detected "'os' imported but unused" (warning, line 1)

[PASS]  AC3a: Missing required field → 422                            LIVE
  Given: Body with code only, no language field
  When:  POST /mcp/tools/execute-static-analysis
  Then:  HTTP 422 (FastAPI schema validation)
  Got:   422

[PASS]  AC3b: Unsupported language → 400 with supported list          LIVE
  Given: language="javascript"
  When:  POST /mcp/tools/execute-static-analysis
  Then:  HTTP 400, detail names unsupported language + supported list
  Got:   400 "Unsupported language: 'javascript'. Supported: ['python']"

[PASS]  AC3c: Empty code → 400                                        LIVE
  Given: code=""
  When:  POST /mcp/tools/execute-static-analysis
  Then:  HTTP 400, detail "'code' must not be empty"
  Got:   400 "'code' must not be empty"

[PASS]  AC4: Syntax error — Stage 1 catches, no further analysis      LIVE
  Given: code="def foo("
  When:  POST /mcp/tools/execute-static-analysis
  Then:  HTTP 200, one issue: severity=error, type=syntax, line=1
  Got:   200 — {"severity":"error","type":"syntax","line":1,"message":"'(' was never closed"}

[PASS]  AC5a: Security finding — bandit B105 hardcoded password       LIVE
  Given: code="password = 'secret123'"
  When:  POST /mcp/tools/execute-static-analysis
  Then:  HTTP 200, bandit issue with test_id B105
  Got:   200 — {"severity":"low","type":"bandit","line":1,"message":"Possible hardcoded password: 'secret123'","test_id":"B105"}

[PASS]  AC5b: Quality finding — pyflakes unused imports               LIVE
  Given: code="import os\nimport sys\n"
  When:  POST /mcp/tools/execute-static-analysis
  Then:  HTTP 200, two pyflakes warnings (one per import)
  Got:   200 — 2 warnings: 'os' imported but unused, 'sys' imported but unused
```

---

## Data Integrity / Pre-existing Issues

None applicable — no database in this project. Hedera ledger is the source of truth
(used in E2+).

---

## Deferred

| Item | Reason |
|---|---|
| S1.2 AC3: Log output key leak check | No log capture tooling in place yet. Mitigated: config.py never passes key values to any logging call. |
| `uvicorn` script shebang | Shebang points to old path (`/...Hedera/mcp-server/...`). Workaround: always invoke as `python3.12 -m uvicorn`. Not a functional issue. |

---

## Sign-off

Verified by: Claude Code (automated)  
14 LIVE checks, 1 STATIC check, 0 BLOCKED.  
Epic E1 — **PASS**. Ready for E2 (x402 pay-per-call rail).
