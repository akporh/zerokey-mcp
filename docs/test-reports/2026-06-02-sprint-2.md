# Sprint 2 Test Report — 2026-06-02

## Summary

| Metric | Value |
|---|---|
| Sprint | 2 (x402 Pay-Per-Call Rail) |
| Date | 2026-06-02 |
| LIVE checks | 14 |
| STATIC checks | 4 |
| BLOCKED | 0 |
| Overall verdict | **PASS** |

---

## Environment

- Server: FastAPI + Uvicorn, port 8000
- Python: 3.12 (.venv)
- Network: Hedera testnet
- Mirror Node: https://testnet.mirrornode.hedera.com
- Server account: `0.0.9082590`
- Agent account: `0.0.9089637`
- `.env` loaded, all required keys present

---

## Sprint Items Verified

### S2.1 — x402 Middleware: Issue 402 Challenge

| # | Acceptance Criterion | Result | Type |
|---|---|---|---|
| 1 | `POST /mcp/tools/*` without receipt → HTTP 402 | **PASS** | LIVE |
| 2 | Response headers: `x402-Invoice-Account`, `x402-Invoice-Amount`, `x402-Invoice-Reference` all present | **PASS** | LIVE |
| 3 | Response body: `{ "error": "payment_required", "invoice": { "account", "amount", "reference" } }` | **PASS** | LIVE |
| 4 | UUID generated fresh per request — two requests produce two distinct UUIDs | **PASS** | LIVE |
| 5 | Requests with `x402-Payment-Receipt` header pass to validation (not re-challenged) | **PASS** | LIVE |
| 6 | `POST /mcp/tools` (list, no trailing slash) not intercepted → 200 | **PASS** | LIVE |
| 7 | `GET /health` not intercepted → 200 | **PASS** | LIVE |

```
Given: POST /mcp/tools/execute-static-analysis, no x402-Payment-Receipt header
When:  request sent
Then:  HTTP 402, headers present, body with payment_required
Got:   HTTP 402, x402-invoice-account: 0.0.9082590, x402-invoice-amount: 0.50000000,
       x402-invoice-reference: <uuid>, body {"error":"payment_required","invoice":{...}}
```

---

### S3.1 — Async Mirror Node Query

| # | Acceptance Criterion | Result | Type |
|---|---|---|---|
| 1 | `MIRROR_NODE_URL` loaded from `.env` | **PASS** | LIVE |
| 2 | `fetch_transaction` is `async def`, uses `httpx.AsyncClient` | **STATIC PASS** | STATIC |
| 3 | Timeout set to 10 seconds | **STATIC PASS** | STATIC |
| 4 | Non-existent tx ID → `payment_invalid` (mirror_node_error path exercised) | **PASS** | LIVE |

```
Given: Receipt with non-existent tx ID (0.0.9089637@0000000001.000000000)
When:  POST /mcp/tools/execute-static-analysis with x402-Payment-Receipt header
Then:  HTTP 402 { "error": "payment_invalid" }
Got:   HTTP 402 { "error": "payment_invalid" } ✓
```

---

### S3.2 — Triple-Check Receipt Validation

| # | Acceptance Criterion | Result | Type |
|---|---|---|---|
| 1 | UUID unknown → `payment_invalid` (not 500) | **PASS** | LIVE |
| 2 | UUID known but wrong UUID in receipt → `payment_invalid` | **PASS** | LIVE |
| 3 | Failure reason not leaked to client — only `"payment_invalid"` returned | **PASS** | LIVE |
| 4 | All three checks: tx SUCCESS + correct amount + memo UUID | **PASS** | LIVE (end-to-end) |

```
Given: Receipt with known pending UUID but mismatched UUID in memo field
When:  POST with x402-Payment-Receipt: 0.0.9089637@<ts>:00000000-0000-...
Then:  HTTP 402 { "error": "payment_invalid" } — no detail about which check failed
Got:   {"error": "payment_invalid"} — ✓ no check detail leaked
```

---

### S2.2 — Client: CryptoTransfer with UUID Memo

| # | Acceptance Criterion | Result | Type |
|---|---|---|---|
| 1 | CryptoTransfer broadcast to Hedera testnet with correct destination + amount + memo | **PASS** | LIVE |
| 2 | Hedera Transaction ID returned | **PASS** | LIVE |
| 3 | Broadcast failure surfaces clearly | **STATIC PASS** | STATIC |

```
Given: Invoice from Step 1 (account 0.0.9082590, amount 0.50000000, reference UUID)
When:  hiero_sdk_python TransferTransaction executed from agent account 0.0.9089637
Then:  Tx broadcast, tx_id returned
Got:   Transaction ID: 0.0.9089637@1780399960.681129932
       Hashscan: https://hashscan.io/testnet/transaction/0.0.9089637@1780399960.681129932
```

---

### S2.3 — Accept Retry + Execute Tool

| # | Acceptance Criterion | Result | Type |
|---|---|---|---|
| 1 | Valid receipt → Mirror Node triple-check → HTTP 200 + tool result | **PASS** | LIVE |
| 2 | Validation fail → HTTP 402 re-issued (not 500) | **PASS** | LIVE |
| 3 | UUID replay blocked — consumed UUID returns HTTP 402 | **PASS** | LIVE |
| 4 | Tool execution does not start before validation confirmed | **STATIC PASS** | STATIC |

```
Given: Valid CryptoTransfer tx on testnet, pending invoice UUID
When:  POST with x402-Payment-Receipt: {tx_id}:{uuid}, 5s propagation wait
Then:  HTTP 200, tool result with analysis summary
Got:   HTTP 200, {'total': 3, 'errors': 0, 'warnings': 2, 'security': 1} ✓

Given: Same receipt re-submitted
When:  POST with same x402-Payment-Receipt header
Then:  HTTP 402 { "error": "payment_invalid" }
Got:   HTTP 402 {"error": "payment_invalid"} ✓
```

---

## End-to-End Live Run (Full 4-Step Test Client)

Run 1 (implementation session):
```
Tx: 0.0.9089637@1780399719.931061029 — ALL STEPS PASSED
```

Run 2 (tester session, independent re-run):
```
Tx: 0.0.9089637@1780399960.681129932 — ALL STEPS PASSED
```

Both runs independently verified on Hedera testnet. Mirror Node triple-check passed in both.

---

## Security Spot-Check

| Check | Result |
|---|---|
| No private key in any HTTP response body or header | PASS — LIVE |
| No API key in any HTTP response body or header | PASS — LIVE |
| Failure reason (uuid_mismatch, tx_not_success, etc.) not returned to client | PASS — LIVE |

---

## Known Pre-Existing Issues

None. This is a greenfield sprint.

---

## Deferred

| Item | Reason |
|---|---|
| UUID TTL expiry (5-minute window) | Would require either `time.sleep(300)` or mocking `time.time()`. TTL logic is trivial (module-level comparison) and verified by code review. Excluded from LIVE checks to avoid a 5-minute test wait. |
| CryptoTransfer broadcast-failure path (S2.2 AC3) | Cannot be triggered deterministically on testnet without network disruption. Code review confirms exception is surfaced, not swallowed. |
| Concurrent load test (multiple simultaneous invoice requests) | Out of scope for MVP sprint. Stateless dict is process-scoped and single-worker. |

---

## Sign-off

Verified by: Claude Code (automated)
LIVE checks: 14 / STATIC checks: 4 / BLOCKED: 0
Verdict: **PASS**
