{
  "_readme": "Replace the two ABSOLUTE_PATH placeholders with the real path to this repo on your machine. Replace the two Hedera values with your testnet account ID and ED25519 private key.",
  "mcpServers": {
    "zerokey": {
      "command": "ABSOLUTE_PATH/mcp-server/.venv/bin/python3.12",
      "args": ["-m", "mcp_server"],
      "cwd": "ABSOLUTE_PATH/mcp-server",
      "env": {
        "HEDERA_ACCOUNT_ID": "YOUR_TESTNET_ACCOUNT_ID",
        "HEDERA_PRIVATE_KEY": "YOUR_ED25519_PRIVATE_KEY",
        "HEDERA_NETWORK": "testnet"
      }
    }
  }
}
